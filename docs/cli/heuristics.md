# FHOPS CLI Heuristic Controls

This document captures the CLI-facing controls for heuristic solvers
(`sa`, `ils`, `tabu`, etc.), including flags, presets, and examples. It
supersedes the high-level sketches in `notes/cli_heuristic_upgrade_notes.md`.

Status: draft (extracted from `notes/cli_heuristic_upgrade_notes.md`).

